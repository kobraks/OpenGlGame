#pragma once

#include "Engine/Core/Base.h"

namespace Engine {
	namespace Stream {
		using SizeType = uint64_t;

		using StringSize = uint32_t;
		using BufferSize = uint64_t;
		using ArraySize = uint32_t;
		using MapSize = uint32_t;
	}
}